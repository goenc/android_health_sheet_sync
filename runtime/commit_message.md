上部歩数を当日値表示へ修正

理由:
日付が変わった後も前日の保存済み歩数が上部へ表示されるため、端末のローカル日付に対応する当日値へ限定するため

変更点:
- 上部の歩数を対象日のレコードだけから解決
- 当日データがない場合に0歩を表示
- 次のローカル日付境界後に画面表示日を更新
- 当日値解決処理のJUnit 4テストを追加

確認内容:
- `gradlew.bat :app:testDebugUnitTest` 成功
- `gradlew.bat :app:assembleDebug` 成功
- `gradlew.bat :app:lintDebug` 成功
- `git diff --check` 成功（改行警告のみ）
- 端末へ `adb install -r` 成功、起動後UIで `歩数 5522歩` と他上部項目を確認
- 実機の日付境界、当日レコードなしの0歩、当日同期後の更新は未確認
