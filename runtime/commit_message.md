距離権限不足時の取得導線を修正

理由:
既存利用者に距離の読み取り権限が追加で必要になっても、データ更新や再起動だけでは権限要求が表示されず距離を取得できない問題を解消するため

変更点:
- 初回読込時に不足しているヘルスコネクト権限を一度だけ自動要求
- データ更新時に不足権限があれば同期より先に権限要求を表示
- 同じ権限構成で自動要求を繰り返さないよう要求済み状態を保存

確認内容:
- 単体テスト成功
- デバッグビルド成功
- 実機へデータを保持したまま上書き更新
- 実機で距離三万六千五百三十二件の完全同期を確認
- 起動二秒後と八秒後に距離四・七キロメートルの表示を確認
