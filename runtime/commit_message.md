腹囲グラフの表示範囲と上限外描画を修正

理由:
腹囲グラフの基準範囲を75cmから80cmへ整理し、範囲外の値が他のグラフ領域へ飛び出さないようにするため

変更点:
- 腹囲グラフの下限を70cmから75cmへ変更
- 腹囲グラフの上限外の線・点を基準領域内でクリップ
- 最新の腹囲数値ラベルは範囲外でも見える位置へ配置
- グラフ上下の描画位置比率は維持

確認内容:
- `gradlew.bat :app:testDebugUnitTest` 成功
- `gradlew.bat :app:assembleDebug` 成功
- `gradlew.bat :app:lintDebug` 成功
- `git diff --check` 成功（改行警告のみ）
- 接続端末へデバッグAPKをインストールし、メイン画面の起動成功を確認
- 起動後の致命的例外なし
